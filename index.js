const m = require ("moment")
const _ = require ("lodash")
const c = require ("chalk")
const ms = require ("moment-range")

const moment = moment-range.extendMoment(M)

let year = moment().range("year")

_.foreach(array.by("months")), month => {
    



}